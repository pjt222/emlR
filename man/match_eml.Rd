% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07_simplify.R
\name{match_eml}
\alias{match_eml}
\title{Pattern-match an EML expression against a meta-variable pattern}
\usage{
match_eml(expr, pattern, bindings = list())
}
\arguments{
\item{expr}{the expression to match.}

\item{pattern}{the pattern.}

\item{bindings}{named list of meta-variable bindings to extend
(used by the recursive walk; callers usually leave this at the
default empty list).}
}
\value{
Named list of bindings (possibly empty if the pattern has no
meta-variables but matches), or \code{NULL} on no match.
}
\description{
Walks \code{expr} and \code{pattern} in parallel. A name in \code{pattern} whose
printed form starts with \verb{_} is a meta-variable that binds to
whatever subexpression sits in the corresponding position of \code{expr};
subsequent occurrences of the same meta-variable must bind to the
identical subexpression. Atoms and non-metavar names must match
exactly.
}
\examples{
match_eml(quote(eml(1, x)),       quote(eml(1, `_x`)))   # _x = x
match_eml(quote(eml(x, x)),       quote(eml(`_x`, `_x`))) # _x = x
match_eml(quote(eml(x, y)),       quote(eml(`_x`, `_x`))) # NULL
match_eml(quote(eml(log(a), 1)),  quote(eml(log(`_x`), 1))) # _x = a
}
