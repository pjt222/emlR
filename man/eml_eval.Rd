% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05_eval.R
\name{eml_eval}
\alias{eml_eval}
\title{Evaluate an EML expression at variable bindings}
\usage{
eml_eval(expr, vars = list(), real = FALSE, tol = 1e-08)
}
\arguments{
\item{expr}{an EML expression.}

\item{vars}{named list of variable bindings, e.g. \code{list(x = 2)}.}

\item{real}{logical; if \code{TRUE}, return the real part with an
imaginary-residue check.}

\item{tol}{numeric tolerance for the residue check, or \code{NA} to skip.}
}
\value{
Complex (or numeric, if \code{real = TRUE}).
}
\description{
Builds an environment containing the \code{eml} operator and the
user-supplied bindings, then defers to \link[base:eval]{base::eval}. Vectorised
inputs are supported transparently — if \code{vars$x} is length N, the
result is length N.
}
\details{
By default the result is complex (the principal-branch domain of
EML). Set \code{real = TRUE} to return \code{Re(z)} after asserting the
imaginary residue is below \code{tol}.
}
\examples{
# exp(2)
eml_eval(quote(eml(x, 1)), list(x = 2), real = TRUE)

# Vectorised: log evaluated at 30 points
xs <- seq(0.5, 5, length.out = 30)
lx <- eml_eval(quote(eml(1, eml(eml(1, x), 1))),
               list(x = xs), real = TRUE)
max(abs(lx - log(xs)))
}
\seealso{
\code{\link[=compile_eml]{compile_eml()}} / \code{\link[=run_bytecode]{run_bytecode()}} for the vectorised
stack-machine evaluator with the same semantics.
}
