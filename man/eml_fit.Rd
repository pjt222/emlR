% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09_master.R
\name{eml_fit}
\alias{eml_fit}
\title{Fit a depth-n master formula to data (symbolic regression)}
\usage{
eml_fit(
  x,
  y,
  depth = 3L,
  parameterization = c("simplex", "direct"),
  method = "L-BFGS-B",
  maxit = 1000L,
  n_restarts = 1L,
  seed = 42L
)
}
\arguments{
\item{x}{numeric vector of inputs.}

\item{y}{numeric vector of targets.}

\item{depth}{integer >= 1.}

\item{parameterization}{either \code{"simplex"} (logits → per-slot
softmax; the paper's choice) or \code{"direct"} (raw scaling).}

\item{method}{\code{optim} method; defaults to \code{"L-BFGS-B"}.}

\item{maxit}{maximum optimiser iterations.}

\item{n_restarts}{number of independent random restarts; the best is
returned. "Best" is the smallest \strong{continuous} final loss — for
exact symbolic recovery this coincides with the snapping vertex when
the global optimum sits at a grammar vertex (as it does for the
paper's \code{exp}/\code{log} targets); it is not selected on \code{snap_mse}.}

\item{seed}{RNG seed for reproducibility (each restart shifts it by 1).}
}
\value{
List with \code{par}, \code{theta}, \code{theta_snap}, \code{pred}, \code{pred_snap},
\code{final_value}, \code{snap_mse}, \code{n_restarts_run}, \code{best_seed}.
}
\description{
Replaces v1's SANN with L-BFGS-B plus analytic gradients via
\link[Deriv:Deriv]{Deriv::Deriv} applied to the eml-expanded master expression. After
convergence, snaps each slot to its argmax one-hot vertex and
reports the snapped MSE — values at machine epsilon squared
indicate exact symbolic recovery.
}
\examples{
\donttest{
xs <- seq(0.5, 5, length.out = 30)
ys <- log(xs)
fit <- eml_fit(xs, ys,
  depth = 3, n_restarts = 2,
  maxit = 200, seed = 1
)
fit$snap_mse # ~ 0 indicates exact symbolic recovery of log(x)
}
}
\seealso{
Other eml_master: 
\code{\link{build_master}()},
\code{\link{master_n_params}()},
\code{\link{snap_master_params}()},
\code{\link{theta_for_exp}()},
\code{\link{theta_for_log}()},
\code{\link{unpack_master_params}()}
}
\concept{eml_master}
