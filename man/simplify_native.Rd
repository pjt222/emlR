% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07_simplify.R
\name{simplify_native}
\alias{simplify_native}
\title{Simplify by collapsing recognised EML patterns to base-R primitives}
\usage{
simplify_native(expr, include_euler = TRUE, trace = FALSE)
}
\arguments{
\item{expr}{an EML expression (or any R \code{call} for re-application).}

\item{include_euler}{include the Euler rules E1 (\code{sin}) and E2
(\code{cos}). Default \code{TRUE}.}

\item{trace}{if \code{TRUE}, return a list \verb{(result, rules, intermediates)}
capturing every rule firing in order. For debugging only.}
}
\value{
The simplified expression, or — if \code{trace = TRUE} — a list
containing the simplified expression plus the firing trace.
}
\description{
Applies a top-down rewrite system to fixed point. The output may
contain \code{exp}, \code{log}, \code{+}, \code{-}, \code{*}, \code{/}, \code{^}, and (with
\code{include_euler = TRUE}) \code{sin}, \code{cos} — anything \code{eval()} can handle.
}
\details{
Rule list (SPEC §3.3, with order tuned to ensure N3 fires before N1
on the structured \code{log} pattern):
\describe{
\item{N3}{\verb{eml(1, eml(eml(1, _x), 1))} → \verb{log(_x)}}
\item{N4}{\verb{eml(log(_x), exp(_y))} → \verb{_x - _y}}
\item{N5}{\verb{eml(log(_x), 1)} → \verb{_x}}
\item{N7}{\verb{eml(_x, exp(_y))} → \verb{exp(_x) - _y}}
\item{N6}{\verb{eml(0, _y)} → \verb{1 - log(_y)}}
\item{N2}{\code{eml(1, 1)} → \code{exp(1)}}
\item{N1}{\verb{eml(_x, 1)} → \verb{exp(_x)}}
\item{C-log-exp}{\verb{log(exp(_x))} → \verb{_x}}
\item{C-exp-log}{\verb{exp(log(_x))} → \verb{_x}}
}
Constant subtrees are folded as a final step.
}
\examples{
simplify_native(quote(eml(x, 1))) # exp(x)
simplify_native(quote(eml(1, eml(eml(1, x), 1)))) # log(x)
simplify_native(quote(eml(log(a), exp(b)))) # a - b
}
\seealso{
\code{\link[=simplify_eml]{simplify_eml()}} for the mode that stays inside the EML
grammar (constant folding only); \code{\link[=eml_catalog]{eml_catalog()}} / \code{\link[=verify_catalog]{verify_catalog()}}
for the test surface this rule set is tuned against.
}
