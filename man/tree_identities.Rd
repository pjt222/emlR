% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_identities.R
\name{tree_e}
\alias{tree_e}
\alias{tree_one}
\alias{tree_exp}
\alias{tree_ln}
\alias{tree_log}
\alias{tree_sub}
\alias{tree_zero}
\alias{tree_neg_one}
\alias{tree_minus}
\alias{tree_add}
\alias{tree_mul}
\alias{tree_div}
\alias{tree_pow}
\alias{tree_two}
\alias{tree_i}
\alias{tree_pi}
\alias{tree_sqrt}
\alias{tree_sin}
\alias{tree_cos}
\alias{tree_identities}
\title{EML expressions for the elementary functions and constants
implemented in this release}
\usage{
tree_e()

tree_one()

tree_exp(x = "x")

tree_ln(x = "x")

tree_log(x = "x")

tree_sub(x = "x", y = "y")

tree_zero()

tree_neg_one()

tree_minus(x = "x")

tree_add(x = "x", y = "y")

tree_mul(x = "x", y = "y")

tree_div(x = "x", y = "y")

tree_pow(x = "x", y = "y")

tree_two()

tree_i()

tree_pi()

tree_sqrt(x = "x")

tree_sin(x = "x")

tree_cos(x = "x")
}
\arguments{
\item{x, y}{character (variable name) or an EML expression to embed.
Univariate constructors take only \code{x}; bivariate constructors
(\code{tree_add}, \code{tree_sub}, \code{tree_mul}, \code{tree_div}, \code{tree_pow}) take
both. Defaults are \code{"x"} and \code{"y"}.}
}
\value{
An EML expression.
}
\description{
Each entry is a closed \code{call} (or literal) representing the named
primitive in pure-EML form. The catalog covers the 18 entries
listed in \code{\link[=eml_catalog]{eml_catalog()}}: the seven constants \code{one}, \code{e}, \code{zero},
\code{neg_one}, \code{two}, \code{i}, \code{pi}, the unary primitives \code{exp}, \code{log},
\code{minus}, \code{sqrt}, \code{sin}, \code{cos}, and the binary primitives \code{add},
\code{sub}, \code{mul}, \code{div}, \code{pow}. Higher trigonometric primitives
(\code{tan}, \code{asin}, \code{acos}, \code{atan}) and hyperbolics (\code{sinh}, \code{cosh},
\code{tanh}) are constructible from the same building blocks but are
not shipped as named entries. Constructions are correct but not
K-optimal — see the paper's Table 4 for the smaller forms found
by exhaustive search.
}
\examples{
tree_log("x") # eml(1, eml(eml(1, x), 1))  — paper Eq. 5
tree_exp("x") # eml(x, 1)
tree_add("x", "y")
simplify_native(tree_log("x")) # log(x)
}
\seealso{
\code{\link[=eml_catalog]{eml_catalog()}} for the catalog as a named list,
\code{\link[=verify_catalog]{verify_catalog()}} for end-to-end numerical verification, and
\code{\link[=simplify_native]{simplify_native()}} for collapsing these expressions to base-R
primitives.
}
