% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_identities.R
\name{verify_catalog}
\alias{verify_catalog}
\title{Verify the catalog: every entry evaluates to its known value}
\usage{
verify_catalog(tol = 1e-08, verbose = TRUE)
}
\arguments{
\item{tol}{numeric tolerance.}

\item{verbose}{if \code{TRUE}, print one line per entry.}
}
\value{
Invisibly, a logical scalar (\code{TRUE} iff every entry passed).
}
\description{
Evaluates each entry of \code{\link[=eml_catalog]{eml_catalog()}} at the test bindings from
SPEC TESTS T2 and checks the result against the known mathematical
value to numerical tolerance. Pure numeric verification — does NOT
require the simplifier to recognise the structural form.
}
\examples{
verify_catalog()
}
\seealso{
\code{\link[=eml_catalog]{eml_catalog()}} for the catalog itself; \code{\link[=simplify_native]{simplify_native()}}
for the structural verifier (every entry should collapse to its
base-R primitive).
}
