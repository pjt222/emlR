% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07_simplify.R
\name{simplify_eml}
\alias{simplify_eml}
\title{Simplify an EML expression, keeping the EML grammar intact}
\usage{
simplify_eml(expr)
}
\arguments{
\item{expr}{an EML expression.}
}
\value{
An EML expression (literal or call).
}
\description{
Constant-folds subtrees that contain no free variables; otherwise
recurses into the children. Preserves every \code{eml} call that has at
least one free variable in either subtree, so the simplified form is
still drawn from the EML grammar (literals, names, and \code{eml} nodes
only). Idempotent on every catalog entry.
}
\examples{
simplify_eml(quote(eml(1, 1))) # numeric e (folded)
simplify_eml(quote(eml(x, 1))) # unchanged
simplify_eml(quote(eml(x, eml(1, 1)))) # eml(x, e)
}
\seealso{
\code{\link[=simplify_native]{simplify_native()}} for the mode that collapses to base-R
primitives (use this for inspection/verification; use
\code{simplify_eml} for SR training where the EML grammar must be
preserved for completeness).
}
