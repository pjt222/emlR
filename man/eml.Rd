% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_core.R
\name{eml}
\alias{eml}
\title{The EML operator: \code{eml(x, y) = exp(x) - log(y)}}
\usage{
eml(x, y)
}
\arguments{
\item{x, y}{numeric or complex (length 1 or vectors of equal length).}
}
\value{
Complex.
}
\description{
Evaluates over the complex plane using R's principal-branch \code{log}.
Inputs are coerced to complex so that \code{log(-1)}, \code{exp} of a complex
argument, and the extended-real values used by some catalog
constructions (\code{log(0) = -Inf}, \code{exp(-Inf) = 0}) all resolve correctly.
}
\examples{
eml(1, 1)                    # exp(1) - log(1) = e
eml(0, -1)                   # 1 - i*pi    (principal-branch log)
Re(eml(-Inf, exp(1)))        # -1, via extended-real semantics
}
\seealso{
\code{\link[=eml_real]{eml_real()}} for the real-valued wrapper, \code{\link[=eml_eval]{eml_eval()}} for
evaluating an EML expression with variable bindings.
}
