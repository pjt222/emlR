% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/99_compat_v1.R
\name{eml_master_eval}
\alias{eml_master_eval}
\title{Deprecated: legacy depth-n master evaluator}
\usage{
eml_master_eval(depth, x, theta)
}
\arguments{
\item{depth}{integer >= 1.}

\item{x}{numeric or complex input.}

\item{theta}{either a v1-style list of per-slot lists, or a v2-style
flat numeric vector of length \code{master_n_params(depth)}.}
}
\value{
Complex vector.
}
\description{
v1's \code{eml_master_eval(depth, x, theta)} accepted a list of per-slot
lists with \code{alpha} / \code{beta} / \code{gamma} entries. The shim converts to
v2's flat parameter vector and dispatches through \code{\link[=build_master]{build_master()}} +
\link[base:eval]{base::eval}. v2 callers should use that pipeline directly.
}
