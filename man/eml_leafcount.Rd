% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_inspect.R
\name{eml_leafcount}
\alias{eml_leafcount}
\title{Leaf count of an EML expression (v1 semantics)}
\usage{
eml_leafcount(expr)
}
\arguments{
\item{expr}{an EML expression.}
}
\value{
Integer scalar.
}
\description{
Counts only the leaves — literals and variables — exactly as v1's
\code{eml_K()} did. Provided for backwards compatibility; the paper's K
is total node count, so prefer \code{\link[=eml_K]{eml_K()}} in new code.
}
\examples{
eml_leafcount(quote(eml(1, eml(eml(1, x), 1))))      # 4 (v1 behaviour)
}
