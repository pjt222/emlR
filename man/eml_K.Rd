% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_inspect.R
\name{eml_K}
\alias{eml_K}
\title{Total node count K of an EML expression (paper definition)}
\usage{
eml_K(expr)
}
\arguments{
\item{expr}{an EML expression.}
}
\value{
Integer scalar.
}
\description{
Counts every node in the tree — both leaves (literals and variables)
and interior \code{eml} nodes. This matches Odrzywolek (2026), Table 4 and
Eq. 5: for \code{log(x) = eml(1, eml(eml(1, x), 1))}, \code{eml_K} returns 7.
}
\details{
This is a behaviour change from v1, which returned the leaf count
(4 for the same expression). v1 semantics are still available via
\code{\link[=eml_leafcount]{eml_leafcount()}}. See \code{ADR-001-K-counting.md} in the spec bundle.
}
\examples{
eml_K(quote(eml(1, 1)))                              # 3 (e)
eml_K(quote(eml(x, 1)))                              # 3 (exp)
eml_K(quote(eml(1, eml(eml(1, x), 1))))              # 7 (log, paper Eq. 5)
}
