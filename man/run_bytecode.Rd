% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_compile.R
\name{run_bytecode}
\alias{run_bytecode}
\title{Run compiled EML bytecode at variable bindings}
\usage{
run_bytecode(bc, vars = list())
}
\arguments{
\item{bc}{compiled bytecode object from \code{\link[=compile_eml]{compile_eml()}}.}

\item{vars}{named list of variable bindings.}
}
\value{
Complex vector.
}
\description{
Stack-machine evaluator. The stack is a preallocated list of complex
vectors; each VAR slot can carry a length-N input, in which case
every interior node is computed elementwise via vectorised
\link[base:Log]{base::exp} and \link[base:Log]{base::log}. Returns a complex vector matching the
input length.
}
\details{
Same semantics as \code{\link[=eml_eval]{eml_eval()}}, including principal-branch and
extended-real conventions.
}
\examples{
bc <- compile_eml(quote(eml(1, eml(eml(1, x), 1))))
xs <- seq(0.5, 5, length.out = 30)
Re(run_bytecode(bc, list(x = xs)))     # log(xs)
}
\seealso{
\code{\link[=compile_eml]{compile_eml()}} for producing the bytecode object;
\code{\link[=eml_eval]{eml_eval()}} for the slower tree-walking evaluator.
}
