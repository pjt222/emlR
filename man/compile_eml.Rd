% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_compile.R
\name{compile_eml}
\alias{compile_eml}
\title{Compile an EML expression to bytecode}
\usage{
compile_eml(expr)
}
\arguments{
\item{expr}{an EML expression.}
}
\value{
A list with elements:
\describe{
\item{\code{ops}}{integer vector of opcodes (0 LIT, 1 VAR, 2 EML).}
\item{\code{args}}{integer vector of opcode arguments (constant index for
LIT, variable index for VAR, 0 for EML).}
\item{\code{consts}}{complex vector — the deduplicated constants pool.}
\item{\code{vars}}{character vector — variable names in declaration order.}
\item{\code{max_stack}}{integer — the peak stack depth required.}
\item{\code{source}}{the original \code{expr}, for inspection.}
}
}
\description{
Produces a flat instruction stream suitable for \code{\link[=run_bytecode]{run_bytecode()}}.
Compilation is a post-order tree walk; each leaf emits a LIT or VAR,
each interior node emits an EML. The constants pool deduplicates
identical literals (\code{eml(1, eml(1, x))} has one shared entry for \code{1}).
}
\examples{
bc <- compile_eml(quote(eml(1, eml(eml(1, x), 1))))
bc$ops              # 0 0 1 2 0 2 2
bc$consts           # 1+0i
bc$vars             # "x"
}
\seealso{
\code{\link[=run_bytecode]{run_bytecode()}} for executing the compiled output;
\code{\link[=eml_eval]{eml_eval()}} for the slower tree-walking evaluator.
}
